// Light/Dark Theme button toggle

function changeTheme() {
    body.classList.toggle('light');
};

// Toggles hamburger menu opening and closing

function toggleMenu() {
    body.classList.toggle('show');
};